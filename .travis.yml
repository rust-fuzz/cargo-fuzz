language: rust

rust:
  - stable
  - beta
  - nightly

os:
  - linux

matrix:
  fast_finish: true

notifications:
  email: false

deploy:
  provider: script
  script: test/run.sh
  on:
    branch: master
